function validIPAdresses(string) {

//compter le nbr de chiffres
if(string.length >=4 && string.length <=12){
    //diviser la string en 4 sequence
    //chaque séquence max 3 chiffres
    for(let i = 0;i <3;i++){
        //stocker chaque sequence dans un tableau
    }
    // verifier si chaque sequence  est compris entre 0 et 255 max et que elles ne commencent pas par 0 suivi d'un autre nbr

    //tester la séquence si la séquence est bonne compteur +1

    //si compteur = 4 stocker la séquence
    //concatener chaque sequence sous la forme adresse ip en une variable 
    // stocker la varible dans un tableau final

}

return [];
}
    
    // Merci de ne pas effacer la ligne en dessous.
    exports.validIPAddresses = validIPAddresses;